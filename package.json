{
  "name": "sharedhotels-www",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Hotels — landing page (static) + form email API (Vercel serverless).",
  "engines": { "node": ">=18" }
}
